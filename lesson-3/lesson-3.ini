1/Giải thích các câu lệnh: git commit --amend, git commit --amend -m”<message>”, git restore --staged <file>, git reset HEAD~1.
git commit --amend: Sửa đổi commit gần nhất.
git commit --amend -m"<message>": Thay đổi thông điệp commit gần nhất.
git restore --staged <file>: Loại bỏ một tệp khỏi vùng stage.
git reset HEAD~1: Hủy bỏ commit gần nhất.

2/Hãy tưởng tượng có 1 folder lesson-3 mới được tạo. Hãy liệt kê các file theo từng vùng, liệt kê các commit và message tương ứng sau khi thao tác một loạt các lệnh sau (sẽ rất tuyệt vời nếu bạn không chạy trên máy tính thật, mà bạn chạy trong đầu, sẽ giúp bạn hiểu về Git rõ ràng hơn):
a. Tạo 3 file: file1, file2, file3
Vùng working directory: Các file file1, file2, file3 được tạo ra và nằm trong thư mục làm việc.
Vùng staging:  (trống)
Vùng Repository:  (trống)

b. Chạy lệnh: git init
Vùng working directory: Các file file1, file2, file3 vẫn nằm trong thư mục làm việc.
Vùng staging: Các file file1, file2, file3 được thêm vào vùng staging.
Vùng Repository:  (trống)

c. Chạy lệnh: git commit -m"init project"
Vùng working directory:  (trống)
Vùng staging:  (trống)
Vùng Repository: file file1, file2, file3.

d. Chạy lệnh: git add file1
Vùng working directory: file file2, file3 
Vùng staging: file1, file2, file3
Vùng Repository: file file2, file3 

e. Chạy lệnh: git commit -m"add file"
Vùng working directory: (trống)
Vùng staging: Không có file nào.
Vùng Repository: commit 1  (init project gồm 3 file1 file2 file3), commit 2(add file có file1)


f. Chạy lệnh: git add .
Vùng working directory: (trống)
Vùng staging:  file file2, file3
Vùng Repository: Không có thay đổi, vẫn 2 commit trước đó.

g. Chạy lệnh: git restore --staged file2
Vùng working directory: (trống)
Vùng staging: Chỉ còn file file3.
Vùng Repository: Không có thay đổi, vẫn 2 commit trước đó.

h. Chạy lệnh: git commit -m"add all files"
Vùng working directory: (trống)
Vùng staging:(trống)
Vùng Repository: Có 3 commit: "init project", "add file", và "add all files" 

k. Chạy lệnh: git reset HEAD~1
Vùng working directory:  file3.
Vùng staging: (trống)
Vùng Repository: Xóa commit "add all files", còn 2 commit "iniut project", "add file"

3/Hãy tưởng tượng có 1 folder lesson-3-git mới được tạo ra. Hãy liệt kê các file theo từng vùng sau khi thao tác các lệnh sau:

a/Chạy lệnh git init
Vùng working directory: khởi tạo git Repository, lesson-3-git
Vùng staging:  (trống)
Vùng Repository:  (trống)

b/Tạo file file1.txt, file2.txt, oops/file3.txt.
Vùng working directory: file1.txt, file2.txt, file3.txt nằm trong folder oops
Vùng staging:  (trống)
Vùng Repository:  (trống)

c/Thêm file .gitignore, thêm vào 2 dòng:
c.1/ file1.txt
c.2/ oops/
Vùng working directory:  file2.txt (file1.txt và file3 sẽ bị git bỏ qua vì nằm trong gitignore, file3 nằm trong  folder oops)
Vùng staging:  (trống)
Vùng Repository: (trống)

d/Chạy lệnh: git add .
Vùng working directory: file2.txt
Vùng staging:  file2.txt
Vùng Repository: (trống)

e/Chạy lệnh: git commit -m"init project"
Vùng working directory: (trống)
Vùng staging:  (trống)
Vùng Repository: file2.txt commit "init project"